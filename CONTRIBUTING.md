# Contribuicao

1. Trabalhe em branch de feature a partir de `develop`.
2. Abra Pull Request para `develop` ou `main`.
3. CI deve passar: `terraform fmt`, `validate`, TruffleHog, tfsec.
4. Nunca commite secrets ou arquivos `.tfvars` com credenciais.

Projeto academico FIAP — Tech Challenge Fase 3.
